(ns info.setmy.file.indexing-test
    (:require [clojure.test :refer :all]
              [info.setmy.file.indexing :as indexing]))

(deftest a-test
    (testing "FIXME, I fail."
             (is (= 1 1))))
